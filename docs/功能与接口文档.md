# AI 旅行规划小程序 —— 功能与接口文档

> 适用读者：产品 / 前端开发 / 后端开发
> 后端地址（本地开发）：`http://localhost:8080`
> 最后更新：2026-09-19

---

## 目录

1. [模块业务概述](#一模块业务概述)
2. [系统架构与模块依赖](#二系统架构与模块依赖)
3. [第三方服务依赖清单](#三第三方服务依赖清单)
4. [数据模型与存储设计](#四数据模型与存储设计)
5. [功能详细说明](#五功能详细说明)
6. [通用规范](#六通用规范)
7. [接口文档](#七接口文档)
8. [异常与降级策略](#八异常与降级策略)
9. [联调注意事项](#九联调注意事项)
10. [Roadmap / 待规划](#十roadmap--待规划)

---

## 一、模块业务概述

### 1.1 功能目标

一款「AI 旅行规划」微信小程序：用户输入**城市 / 喜好 / 预算 / 天数 / 体力**，AI 结合真实口碑（小红书、B 站等）+ 本地知识库，自动生成「每日行程 + 地图路线」攻略；到景点后支持**语音导游**（识别景点 + AI 讲解 + 语音问答）。

### 1.2 使用场景

| 场景 | 说明 |
|------|------|
| 出行规划 | 出发前输入需求，AI 生成一份可落地、含坐标的每日行程攻略 |
| 现场游览 | 到景点后定位/拍照识别，AI 生成讲解词语音播报 |
| 途中提问 | 按住说话提问（门票、历史、美食），AI 语音回答，支持多轮追问 |
| 分享传播 | 把攻略分享给好友/朋友圈，或生成小程序码让别人扫码直达 |

### 1.3 核心业务流程

```
登录 → 生成攻略 → 查看/分享攻略 → 语音导游
        │              │                │
    表单页输入     结果页/历史页      到景点识别→讲解/问答
```

### 1.4 个人主体限制（重要约束）

小程序为**个人主体**，以下能力受限，产品设计时需遵守：

| 限制 | 影响 | 当前方案 |
|------|------|---------|
| 无微信支付 | 做不了付费/会员/打赏 | 已排除 |
| 不能选「旅游」类目 | 只能选「工具-信息查询」 | 介绍文案按信息查询写 |
| 不能用微信同声传译插件 | 不能用微信 ASR/TTS | 改用百度智能云语音 |
| 订阅消息仅「一次性」 | 授权一次只能发一条 | 生成完成时发一条即消耗 |

---

## 二、系统架构与模块依赖

### 2.1 技术栈

| 层 | 技术 |
|----|------|
| 前端 | 微信小程序（原生，队友负责） |
| 后端 | Spring Boot 4.0.8（Java 17）+ MyBatis |
| 存储 | MySQL + Redis + Zilliz Cloud（托管 Milvus 向量库） |
| AI | DeepSeek（生成/Function Calling/流式）、硅基流动 embedding、通义千问 qwen-vl（识图） |
| 搜索/地图 | 博查搜索、腾讯地图 WebService |
| 语音 | 百度智能云（ASR/TTS） |
| 对象存储 | 阿里云 OSS（头像） |
| 微信 | 登录 code2session、订阅消息、小程序码 |

### 2.2 后端模块划分

```
com.study.travel_guide
├── controller/     # 接口层（Auth / Trip / Guide / Voice / User / Admin）
├── service/
│   ├── workflow/   # 攻略生成工作流（7 步编排）
│   ├── agent/      # 智能体（ReAct + Function Calling + 工具）
│   ├── rag/        # RAG（embedding / 向量存储 / 检索 / 摄入）
│   ├── guide/      # 语音导游（识别 / 讲解 / 问答 / 知识库 / 种子）
│   ├── voice/      # 百度 ASR/TTS
│   ├── wechat/     # 微信（access_token / 订阅消息 / 小程序码）
│   ├── qwen/       # 通义千问识图
│   ├── memory/     # 用户记忆
│   └── ...         # DeepSeek / 博查 / 腾讯地图 / OSS / Trip / Auth / User
├── mapper/         # MyBatis 数据访问
├── config/         # 配置（JWT 拦截、CORS、OSS、RestClient）
└── common/         # 通用（Result / BizException / JwtUtil / 全局异常）
```

### 2.3 核心数据流（攻略生成）

```
前端表单
  → TripController.generate/stream
  → TripWorkflowService（7 步）
      ├─ RAG 检索   ← Zilliz（向量库）
      ├─ Agent 工具 ← 博查 / RAG / 腾讯地图
      ├─ 生成攻略   ← DeepSeek
      ├─ 反思校验   ← DeepSeek
      ├─ 地理编码   ← 腾讯地图
      └─ 持久化     → MySQL / 摄入 → Zilliz
  → 前端 SSE 流式展示
```

---

## 三、第三方服务依赖清单

> 所有密钥配置在 `travel_guide/src/main/resources/application.yml`（已 gitignore），模板见 `application.yml.example`。

| 服务 | 用途 | 配置项（yml key） | 关键限制 / 额度 | 失败影响 |
|------|------|------------------|----------------|---------|
| **DeepSeek** | 攻略生成、Agent、反思、讲解词、问答 | `deepseek.api-key` / `base-url` / `model` | 按 token 计费，需充值 | 攻略/讲解/问答全部不可用（核心依赖） |
| **博查搜索** | 联网搜索真实口碑 | `bocha.api-key` / `base-url` | 按次计费，有日额度 | 素材缺失，攻略质量下降（可降级） |
| **腾讯地图** | 地点搜索、附近搜索、地理编码 | `tencent.map-key` | 个人认证 1 万次/天 | 坐标缺失、附近景点识别失败（可降级） |
| **Zilliz Cloud** | 向量库（RAG 检索/摄入） | `vectorstore.base-url` / `api-key` / `collection` | 免费额度 | RAG 不可用（已降级为无知识库生成） |
| **硅基流动** | embedding（bge-m3，1024 维） | `embedding.base-url` / `api-key` / `model` | 按 token 计费 | RAG 不可用（同 Zilliz） |
| **通义千问** | qwen-vl 识图（景点识别） | `qwen.api-key` / `base-url` / `model` | 按 token 计费 | 拍照识别不可用（可降级到定位） |
| **百度智能云** | ASR 语音识别 + TTS 合成 | `baidu.api-key` / `secret-key` | 免费额度有限 | 语音输入/播报不可用 |
| **阿里云 OSS** | 头像存储 | `aliyun.oss.*` | 存储 + 流量计费 | 头像上传失败（返回 null，保留旧头像） |
| **微信** | 登录、订阅消息、小程序码 | `wechat.appid` / `secret` / `subscribe-template-id` | access_token 2h 有效 | 登录/订阅/二维码各自受影响 |

**说明**：`workflow.reflect`（反思校验开关）默认 `true`，关闭可省一次 LLM 调用。

---

## 四、数据模型与存储设计

### 4.1 MySQL（`travel_guide` 库）

**`user` 表**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK 自增 | 用户 ID |
| openid | VARCHAR(64) | UNIQUE NOT NULL | 微信 openid |
| nickname | VARCHAR(64) | 可空 | 昵称 |
| avatar_url | VARCHAR(255) | 可空 | 头像 URL（OSS 公网地址） |
| created_at / updated_at | DATETIME | 默认当前时间 | 时间戳 |

**`trip` 表**

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK 自增 | 行程 ID |
| user_id | BIGINT | NOT NULL，索引 | 所属用户 |
| city | VARCHAR(64) | NOT NULL | 城市 |
| preferences | VARCHAR(255) | 可空 | 喜好 |
| budget | VARCHAR(64) | 可空 | 预算 |
| days | INT | NOT NULL | 天数（1-15） |
| energy_level | VARCHAR(16) | 可空 | 体力档位（easy/medium/hard） |
| extra | VARCHAR(255) | 可空 | 其他需求 |
| share_token | VARCHAR(64) | 可空 | 分享 token（16 位） |
| status | VARCHAR(16) | 默认 done | 状态 |
| result | JSON | 可空 | 完整攻略 JSON |
| created_at / updated_at | DATETIME | 默认当前时间 | 时间戳 |

### 4.2 Redis

| Key | 用途 | 过期策略 |
|-----|------|---------|
| `guide:session:{sessionId}` | 语音问答会话历史（JSON 数组） | TTL 2 小时，保留最近 10 条 |

### 4.3 Zilliz 向量库（两个集合）

| 集合 | 用途 | 过滤字段 |
|------|------|---------|
| `travel_guide` | 攻略知识库（历史优质攻略） | `city`（按城市过滤） |
| `attraction_guide` | 讲解知识库（景点讲解素材） | `attraction`（按景点过滤） |

- 相似度：COSINE；向量维度：1024（bge-m3）
- 摄入时按 500 字分块，embedding 后写入

---

## 五、功能详细说明

### 5.1 攻略生成（核心功能）

#### 5.1.1 7 步工作流

`TripWorkflowService.generate` / `generateStreaming`，编排如下：

```
输入：city / preferences / budget / days / energyLevel / extra
  │
  1. 输入校验        city 非空，days ∈ [1,15]
  2. RAG 检索        用户记忆 + 知识库（topK=3，按 city 过滤）
  3. Agent 搜集      联网搜索 + 知识库 + 地理编码（1-2 轮工具调用）
  4. 生成攻略        DeepSeek 按固定 JSON schema 生成
  5. 反思校验        检查路线顺路/预算/体力匹配，可开关（workflow.reflect）
  6. 并行地理编码    所有景点并发查坐标（lat/lng）
  7. 持久化 + 摄入    存 MySQL；攻略文本再喂回向量库（越用越聪明）
  └─ 异步：发订阅消息（生成完成提醒）
```

**降级策略**：
- RAG 检索失败 → 降级为「无知识库生成」，仅靠联网搜索 + 大模型
- 反思校验失败 → 保留原攻略（`reflectAndRefine` catch 后返回原 guide）
- 地理编码失败（某个景点没查到坐标）→ 该景点缺 lat/lng，不影响其他
- 订阅消息发送失败 → 仅日志告警，不影响生成结果

#### 5.1.2 RAG（检索增强生成）

```
检索（RetrievalService.retrieve）：
  用户 query → embedding（bge-m3，1024 维）→ Zilliz 向量检索（COSINE，topK=3，按 city 过滤）

摄入（IngestionService.ingest）：
  攻略文本 → 按 500 字分块 → embedding → 写入 travel_guide 集合（带 city 元数据）
```

**关键点**：每次生成的攻略会「喂回」知识库，实现**越用越聪明**——同一城市再次生成时能检索到历史优质攻略。

#### 5.1.3 Agent（智能体）

`AgentService.run` 采用 **ReAct 循环**（推理 + 行动），最多 6 轮：

```
循环：
  → 调 DeepSeek（带 tools 定义）
  → 若模型返回 tool_calls：执行工具，把结果回填，继续循环
  → 若模型返回最终文本：结束，输出「行程素材汇总」
```

三个工具（`Tool` 接口 + `ToolRegistry` 自动注册）：

| 工具 | 名称 | 能力 |
|------|------|------|
| 联网搜索 | `search_web` | 博查搜索真实口碑（小红书/B 站等） |
| 知识库检索 | `search_kb` | 检索本地攻略知识库（可按城市过滤） |
| 地理编码 | `geocode` | 腾讯地图查景点经纬度 |

#### 5.1.4 流式输出（SSE）

`POST /api/trip/generate/stream` 返回 `text/event-stream`，事件：

| type | data | 说明 |
|------|------|------|
| `step` | 字符串 | 进度提示（「正在搜索：杭州 美食」） |
| `token` | 字符串 | 攻略 JSON 生成中的文本片段 |
| `done` | `{tripId}` | 生成完成 |
| `error` | 字符串 | 出错信息 |

#### 5.1.5 用户记忆

`UserMemoryService.buildMemoryContext(userId, 5)` 读取用户最近 5 条行程，提取「去过的城市 / 偏好类型 / 常用体力档位」，注入生成 prompt，实现个性化推荐。

---

### 5.2 语音导游

#### 5.2.1 景点识别（定位优先 + 拍照兜底）

```
identify(lat, lng, image)：
  ① 有坐标 → 腾讯地图 nearby 搜 1km 内 POI → 命中返回 {attraction, source:"location"}
  ② 无坐标/未命中 → qwen-vl 识图 → 返回 {attraction, source:"image"}
  ③ 都失败 → 抛「未能识别景点，请尝试拍照」
```

#### 5.2.2 讲解生成（RAG + 联网 + 大模型）

```
narrate(attraction)：
  RAG 检索（attraction_guide 集合，topK=3，按 attraction 过滤）
  + 博查联网搜索（"景点 历史 典故 简介"）
  → DeepSeek 生成 200-400 字口语化讲解词（禁止 markdown）
```

**降级**：知识库无命中（新景点）→ 仅靠联网搜索 + 大模型。

#### 5.2.3 语音问答（多轮对话 + 记忆）

```
chat(sessionId, question)：
  读 Redis 会话历史（最近 10 条）
  + RAG 检索 + 博查搜索
  → DeepSeek 生成 100-200 字回答
  → 写回 Redis（TTL 2h）
```

首次 `sessionId` 为空，后端生成 UUID 返回，前端保存复用。

#### 5.2.4 语音识别/合成（百度 ASR/TTS）

| 能力 | 接口 | 说明 |
|------|------|------|
| ASR | `/api/voice/asr` | base64 音频 → 文字（要求 wav/16000Hz/单声道） |
| TTS | `/api/voice/tts` | 文字 → base64 mp3 |

---

### 5.3 微信生态

| 功能 | 实现 | 说明 |
|------|------|------|
| 登录 | code2session + JWT | 前端 `wx.login()` 拿 code 换 token |
| 分享 | share token（16 位） | `POST /api/trip/{id}/share` 开启，`GET /api/trip/public/{token}` 公开访问 |
| 小程序码 | getwxacodeunlimit | scene=`token=xxx`，扫码直达结果页 |
| 订阅消息 | 生成完成提醒 | 模板字段：行程名称/日期/路线/备注 |

---

### 5.4 头像（OSS）

`POST /api/user/profile` 接收 base64 头像，上传 OSS，返回公网 URL。**部分更新**：昵称/头像各自有值才更新，避免清空已有字段。

---

## 六、通用规范

### 6.1 统一返回体

```json
{ "code": 0, "message": "ok", "data": { } }
```

- `code = 0` 成功；非 0 失败
- 失败时 `data` 为 null

### 6.2 错误码

| code | 含义 | 触发场景 |
|------|------|---------|
| 401 | 未登录 / token 无效 | 无 token、token 过期/非法 |
| 400 | 参数错误 / 业务错误 | 城市为空、天数超范围、识别失败 |
| 404 | 不存在 | 行程/分享不存在 |
| 500 | 服务器内部错误 | 未知异常 |
| 502 | 第三方调用失败 | AI / 搜索 / 向量库 / 地图 / 语音 / 微信接口失败 |

### 6.3 鉴权

- 除「登录」和「公开分享」外，所有接口需带请求头 `Authorization: Bearer <token>`
- 公开接口：`POST /api/auth/login`、`GET /api/trip/public/{token}`

### 6.4 特殊返回格式

| 接口 | 返回格式 | 前端处理 |
|------|---------|---------|
| `/api/trip/generate/stream` | SSE（`text/event-stream`） | `enableChunked` + `onChunkReceived` |
| `/api/trip/{id}/qrcode` | JSON 内嵌 base64 PNG | `wx.base64ToArrayBuffer` 转临时文件 |
| `/api/voice/tts` | JSON 内嵌 base64 mp3 | 转临时文件 + `InnerAudioContext` 播放 |

### 6.5 字段约束速查

| 字段 | 约束 |
|------|------|
| `days` | 1–15 整数 |
| `energyLevel` | `easy` / `medium` / `hard` |
| 坐标 `lat`/`lng` | GCJ-02 坐标系（与腾讯地图、微信一致） |
| 录音 | wav、16000Hz、单声道 |
| 分享 token | 16 位（scene=`token=`+16=22 字符 < 微信 32 上限） |

---

## 七、接口文档

> 除标注「公开」外，均需 `Authorization: Bearer <token>`。

### 7.1 认证模块

#### POST /api/auth/login（公开）

**接口描述**：微信登录，code 换 token。

**请求体**

| 字段 | 类型 | 必传 | 说明 |
|------|------|------|------|
| code | string | 是 | `wx.login()` 拿到的临时凭证 |

**成功响应**

```json
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "user": { "id": 1, "openid": "xxx", "nickname": null, "avatarUrl": null }
  }
}
```

**业务异常**：`401` code 无效或过期（微信 code 5 分钟有效）；`502` 微信接口调用失败。

---

### 7.2 攻略模块

#### POST /api/trip/generate

**接口描述**：生成攻略（非流式，一次性返回）。

**请求体**（同 `/generate/stream`）

| 字段 | 类型 | 必传 | 说明 | 约束 |
|------|------|------|------|------|
| city | string | 是 | 城市 | 非空 |
| preferences | string | 否 | 喜好 | 自由文本 |
| budget | string | 否 | 总预算 | 自由文本 |
| days | int | 是 | 天数 | 1–15 |
| energyLevel | string | 否 | 体力档位 | easy/medium/hard |
| extra | string | 否 | 其他需求 | 自由文本 |

**成功响应**

```json
{
  "code": 0,
  "data": { "tripId": 1, "result": { "...完整攻略 JSON..." } }
}
```

**业务异常**：`400` 城市为空 / 天数超范围；`500` Agent 达最大迭代次数；`502` AI/搜索/向量库失败。

#### POST /api/trip/generate/stream

**接口描述**：流式生成攻略（SSE）。请求体同上，返回 `text/event-stream`，事件格式见 [5.1.4](#514-流式输出sse)。

#### GET /api/trip/{id}

**接口描述**：行程详情。

**成功响应**

```json
{
  "code": 0,
  "data": {
    "id": 1, "city": "杭州", "days": 3, "energyLevel": "medium",
    "result": { "city": "杭州", "overview": "...", "days": [...], "tips": [...], "sources": [...] },
    "createdAt": "2026-09-16T17:00:00"
  }
}
```

**业务异常**：`404` 行程不存在。

#### GET /api/trip/list

**接口描述**：历史列表（摘要）。

**成功响应**

```json
{
  "code": 0,
  "data": [ { "id": 1, "title": "杭州 3天", "createdAt": "2026-09-16T17:00:00" } ]
}
```

#### DELETE /api/trip/{id}

**接口描述**：删除行程。**业务异常**：`404` 行程不存在。

#### GET /api/trip/{id}/qrcode

**接口描述**：生成攻略小程序码（自动开启分享）。

**成功响应**

```json
{
  "code": 0,
  "data": { "image": "base64 PNG", "contentType": "image/png", "token": "16位分享token" }
}
```

**业务异常**：`404` 行程不存在；`502` 小程序码生成失败（常见：小程序未发布版本）。

#### POST /api/trip/{id}/share

**接口描述**：开启/获取分享 token。**成功响应**：`{ "token": "16位token" }`。

#### DELETE /api/trip/{id}/share

**接口描述**：撤销分享。**业务异常**：`404` 行程不存在。

#### GET /api/trip/public/{token}（公开）

**接口描述**：通过分享 token 公开访问攻略（无需登录）。

**成功响应**

```json
{
  "code": 0,
  "data": { "id": 1, "city": "杭州", "days": 3, "result": { "...完整攻略..." } }
}
```

**业务异常**：`404` 分享不存在或已失效。

---

### 7.3 语音导游模块

#### POST /api/guide/identify

**接口描述**：识别所在景点（定位优先，拍照兜底）。

**请求体**（lat/lng/image 至少传一个）

| 字段 | 类型 | 必传 | 说明 |
|------|------|------|------|
| lat | double | 否 | 纬度（GCJ-02） |
| lng | double | 否 | 经度（GCJ-02） |
| image | string | 否 | 图片 base64 或 URL |

**成功响应**

```json
{ "code": 0, "data": { "attraction": "西湖", "source": "location" } }
```

`source` 取值 `location` / `image`。**业务异常**：`400` 两者都识别失败。

#### POST /api/guide/narrate

**接口描述**：生成讲解词。

**请求体**：`{ "attraction": "西湖" }`

**成功响应**

```json
{ "code": 0, "data": { "attraction": "西湖", "script": "西湖位于杭州……" } }
```

#### POST /api/guide/chat

**接口描述**：语音问答（多轮）。

**请求体**

| 字段 | 类型 | 必传 | 说明 |
|------|------|------|------|
| sessionId | string | 否 | 会话 ID（首次不传） |
| question | string | 是 | 问题 |

**成功响应**

```json
{ "code": 0, "data": { "sessionId": "550e8400-...", "answer": "西湖最著名的传说是白蛇传……" } }
```

---

### 7.4 语音模块

#### POST /api/voice/asr

**接口描述**：语音转文字。

**请求体**：`{ "audio": "base64", "format": "wav" }`（format 默认 wav，音频 16000Hz 单声道）

**成功响应**：`{ "code": 0, "data": { "text": "西湖有什么传说" } }`

**业务异常**：`502` 百度 ASR 调用失败。

#### POST /api/voice/tts

**接口描述**：文字转语音。

**请求体**：`{ "text": "西湖最著名的传说是白蛇传……" }`

**成功响应**：`{ "code": 0, "data": { "audio": "base64 mp3", "contentType": "audio/mp3" } }`

**业务异常**：`400` text 为空；`502` 百度 TTS 失败。

---

### 7.5 用户模块

#### POST /api/user/profile

**接口描述**：更新昵称/头像（部分更新，传哪个改哪个）。

**请求体**

| 字段 | 类型 | 必传 | 说明 |
|------|------|------|------|
| nickname | string | 否 | 昵称 |
| avatar | string | 否 | 头像 base64（可带 data URL 前缀） |

**成功响应**：`{ "code": 0, "data": { "nickname": "xxx", "avatarUrl": "https://...oss.../avatar/..." } }`

---

### 7.6 管理模块

#### POST /api/admin/seed-attractions

**接口描述**：灌入 14 个著名景点的讲解素材（一次性初始化讲解知识库）。

**成功响应**：`{ "code": 0, "data": { "total": 14, "success": 14, "chunks": 70 } }`

---

## 八、异常与降级策略

### 8.1 第三方失败处理原则

| 第三方 | 失败时行为 | 是否阻断主流程 |
|--------|-----------|--------------|
| DeepSeek | 抛 502 | 是（核心） |
| 博查搜索 | 降级：无联网素材，仅知识库+大模型 | 否 |
| 腾讯地图 | 降级：坐标缺失 / 识别降级拍照 | 否 |
| Zilliz + embedding | 降级：无知识库生成 | 否 |
| 通义千问 | 降级：识图失败→定位识别 | 否 |
| 百度语音 | 抛 502 | 语音功能阻断 |
| OSS | 降级：头像上传失败，保留旧头像 | 否 |
| 微信 | 各接口独立失败 | 登录阻断；订阅/二维码各自降级 |

### 8.2 关键降级链

```
景点识别：定位 → 拍照 → 报错
讲解生成：RAG → 联网搜索 → 大模型（RAG 无命中不阻断）
攻略生成：RAG 失败 → 仅 Agent 联网 + 大模型
反思校验：失败 → 保留原攻略
```

### 8.3 异常抛出约定

- 业务异常统一抛 `BizException(code, message)`，由 `GlobalExceptionHandler` 转成 `Result` 返回
- 第三方调用失败统一用 `502`，并在 message 里带第三方名称和 status，便于排查

---

## 九、联调注意事项

### 9.1 登录链路

1. 前端 `wx.login()` 拿 code（5 分钟有效）
2. 调 `/api/auth/login` 换 token，存本地 storage
3. 后续所有请求带 `Authorization: Bearer <token>`

### 9.2 流式接口

- `wx.request` 需 `enableChunked: true` + `onChunkReceived` 接收 SSE
- 收到 `done` 里的 `tripId` 后，调 `GET /api/trip/{id}` 拿最终完整结果

### 9.3 base64 处理

- 小程序码 PNG：`wx.base64ToArrayBuffer` → 写临时文件 → `<image>` 展示
- TTS mp3：同上，转临时文件后 `wx.createInnerAudioContext` 播放

### 9.4 录音参数

- `wx.getRecorderManager` 用 `format: 'wav'`、`sampleRate: 16000`、`numberOfChannels: 1`，否则百度 ASR 识别不准

### 9.5 坐标一致性

- 全程 GCJ-02（微信定位、腾讯地图 API、前端地图组件一致），**无需坐标转换**
- 前端 `wx.getLocation` 拿到的就是 GCJ-02，直接传后端

### 9.6 鉴权拦截

- 后端拦截器放行 `OPTIONS` 请求（预检），其余 `/api/**` 都要 token
- 公开接口：`/api/auth/login`、`/api/trip/public/**`

---

## 十、Roadmap / 待规划

| 特性 | 状态 | 说明 |
|------|------|------|
| OCR 文字识别 | ✅ 已具备 | qwen-vl 识图本身可识别文字（路牌/票据），未单独开接口 |
| 短视频识别 | 🚧 规划中 | 用户拍短视频 → 抽帧 → AI 讲解（复用 qwen-vl + 讲解 + TTS） |
| 自动导游 | 📋 候选 | 走到景点自动讲解（前端位置监听 + 定时触发） |
| 附近景点推荐 | 📋 候选 | 独立「附近景点」接口（复用腾讯地图 nearby 搜索） |
| 增长运营（会员/裂变） | 📋 候选 | 个人主体受限，优先做轻量留存（收藏/分享海报） |
| 工程工具（MCP/监控） | 📋 候选 | 封装攻略能力为 MCP、接入监控 |

---

*文档结束*
